spring:
  datasource:
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5

  jpa:
    show-sql: false
    properties:
      hibernate:
        format_sql: false
        jdbc:
          fetch_size: 500

logging:
  level:
    root: WARN
    com.palmar: INFO
